import React from 'react'

/** Компактная иконка в шапке админки. */
export function Icon() {
  return (
    <span
      style={{
        display: 'grid',
        placeItems: 'center',
        height: 26,
        width: 26,
        borderRadius: 6,
        background: '#c1121f',
        color: '#fff',
        fontWeight: 800,
        fontSize: 15,
      }}
    >
      Л
    </span>
  )
}

export default Icon
